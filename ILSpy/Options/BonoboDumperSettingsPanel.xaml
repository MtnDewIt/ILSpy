<UserControl x:Class="ICSharpCode.ILSpy.Options.BonoboDumperSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
			 xmlns:properties="clr-namespace:ICSharpCode.ILSpy.Properties"
			 xmlns:options="clr-namespace:ICSharpCode.ILSpy.Options"
			 d:DataContext="{d:DesignInstance options:BonoboDumperSettingsViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
	<StackPanel Orientation="Vertical">
		<GroupBox Header="{x:Static properties:Resources.Bonobo_Dumper}">
			<StackPanel Margin="10">
				<DockPanel Margin="0,2,0,2">
					<TextBlock Text="Build" Width="100" VerticalAlignment="Center" Margin="0,0,5,0"/>
					<ComboBox Grid.Row="0" ItemsSource="{Binding Builds}" VerticalContentAlignment="Center" SelectedItem="{Binding Settings.Build}" Grid.Column="1" />
				</DockPanel>
				<DockPanel Margin="0,2,0,2">
					<TextBlock Text="Platform" Width="100" VerticalAlignment="Center" Margin="0,0,5,0"/>
					<ComboBox Grid.Row="0" ItemsSource="{Binding Platforms}" VerticalContentAlignment="Center" SelectedItem="{Binding Settings.Platform}" Grid.Column="1" />
				</DockPanel>
				<DockPanel Margin="0,2,0,2">
					<TextBlock Text="Output Path" Width="100" VerticalAlignment="Center" Margin="0,0,5,0"/>
					<Button DockPanel.Dock="Right" Content="..." Width="30" Margin="5,0,0,0" Command="{Binding OutputCommand}"/>
					<TextBox Text="{Binding Settings.OutputPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" />
				</DockPanel>
				<DockPanel Margin="0,2,0,2">
					<TextBlock Text="Bonobo Path" Width="100" VerticalAlignment="Center" Margin="0,0,5,0"/>
					<Button DockPanel.Dock="Right" Content="..." Width="30" Margin="5,0,0,0" Command="{Binding BonoboCommand}"/>
					<TextBox Text="{Binding Settings.BonoboPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" />
				</DockPanel>
			</StackPanel>
		</GroupBox>
	</StackPanel>
</UserControl>
